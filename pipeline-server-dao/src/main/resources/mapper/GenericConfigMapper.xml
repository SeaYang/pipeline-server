<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ci.pipeline.dao.mapper.GenericConfigMapper">

    <resultMap id="BaseResultMap" type="com.ci.pipeline.dao.entity.GenericConfig">
        <id column="id" property="id"/>
        <result column="config_key" property="configKey"/>
        <result column="config_value" property="configValue"/>
        <result column="value_format" property="valueFormat"/>
        <result column="description" property="description"/>
        <result column="creator" property="creator"/>
        <result column="create_time" property="createTime"/>
        <result column="updater" property="updater"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, config_key, config_value, value_format, description, creator,
        create_time, updater, update_time, deleted
    </sql>

    <select id="listBySearch" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM generic_config
        WHERE deleted = 0
        <if test="configKey != null and configKey != ''">
            AND config_key LIKE CONCAT('%', #{configKey}, '%')
        </if>
        ORDER BY id ASC
    </select>

    <select id="getByKey" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM generic_config
        WHERE deleted = 0
          AND config_key = #{configKey}
        LIMIT 1
    </select>

</mapper>
