<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ci.pipeline.dao.mapper.PipelineRunSnapshotMapper">

    <resultMap id="BaseResultMap" type="com.ci.pipeline.dao.entity.PipelineRunSnapshot">
        <id column="id" property="id"/>
        <result column="pipeline_run_id" property="pipelineRunId"/>
        <result column="detail" property="detail"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, pipeline_run_id, detail, create_time, update_time, deleted
    </sql>

    <!-- 自定义查询不会被逻辑删除插件自动追加 deleted 条件，手动指定 deleted = 0 -->
    <select id="selectByPipelineRunId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pipeline_run_snapshot
        WHERE deleted = 0
          AND pipeline_run_id = #{pipelineRunId}
        LIMIT 1
    </select>

    <update id="updateDetail">
        UPDATE pipeline_run_snapshot
        SET detail = #{detail}
        WHERE deleted = 0
          AND id = #{id}
    </update>

</mapper>
