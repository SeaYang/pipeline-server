<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ci.pipeline.dao.mapper.PipelineCleanRunMapper">

    <resultMap id="BaseResultMap" type="com.ci.pipeline.dao.entity.PipelineCleanRun">
        <id column="id" property="id"/>
        <result column="run_name" property="runName"/>
        <result column="clean_run_name" property="cleanRunName"/>
        <result column="status" property="status"/>
        <result column="end_time" property="endTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, run_name, clean_run_name, status, end_time, create_time, update_time
    </sql>

    <select id="listTimeoutRunning" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pipeline_clean_run
        WHERE status = 'Running' AND create_time &lt; #{deadline}
        LIMIT 100
    </select>

    <update id="casMarkSucceeded">
        UPDATE pipeline_clean_run
        SET status = 'Succeeded', end_time = NOW()
        WHERE id = #{id} AND status = 'Running'
    </update>

    <update id="casMarkFailed">
        UPDATE pipeline_clean_run
        SET status = 'Failed', end_time = NOW()
        WHERE id = #{id} AND status = 'Running'
    </update>

    <update id="updateCleanRunName">
        UPDATE pipeline_clean_run
        SET clean_run_name = #{cleanRunName}
        WHERE id = #{id}
    </update>
</mapper>
